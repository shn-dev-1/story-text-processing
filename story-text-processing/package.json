{
  "name": "story-text-processing-lambda",
  "version": "1.0.0",
  "description": "Lambda function for processing text from SQS queue",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../story_text_processing_lambda_function.zip .",
    "package:lambda": "npm run build && cp package.json dist/ && cd dist && npm install --production --no-optional --no-audit --no-fund --prefer-offline && zip -r ../../story_text_processing_lambda_function.zip ."
  },
  "keywords": [
    "aws",
    "lambda",
    "sqs",
    "story-text-processing"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/lib-dynamodb": "^3.470.0",
    "openai": "^5.13.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^18.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
